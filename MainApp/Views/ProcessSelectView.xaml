<Window x:Class="MainApp.Views.ProcessSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Title="Select process" Height="400" Width="400">
    <StackPanel>
        <ListBox Height="300"
                 ItemsSource="{Binding Processes}" SelectedIndex="{Binding SelectedProcessIndex}" />
        <Button Click="OkButton_OnClick">Ok</Button>
        <Button Click="CancelButton_OnClick">Cancel</Button>
    </StackPanel>
</Window>